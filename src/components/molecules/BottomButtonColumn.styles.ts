import { StyleSheet } from 'react-native-unistyles'

// Only the stack itself. Button appearance comes from AppButton.
export const styles = StyleSheet.create(() => ({
  container: { width: '100%' },
}))
